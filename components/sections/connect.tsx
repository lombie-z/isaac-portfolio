/**
 * SECTION 6 — SOCIALS / END
 * Component: https://21st.dev/@aliimam/components/cosmos-spectrum
 *
 * Spec:
 * - Cosmos/spectrum finale with social icons rendered white against the brand
 *   colour, glowing on hover.
 *
 * TODO(connect-agent): replace with the cosmos-spectrum finale + social icons.
 * Exact social links come from round-2 grilling.
 */
export function ConnectSection() {
  return (
    <section
      id="connect"
      className="flex min-h-screen flex-col items-center justify-center gap-8 bg-brand-deep p-8 text-brand-foreground"
    >
      <h2 className="text-3xl font-semibold tracking-tight md:text-5xl">
        Let&apos;s connect
      </h2>
    </section>
  );
}
