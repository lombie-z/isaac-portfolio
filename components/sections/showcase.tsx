/**
 * SECTION 3 — DEV SHOWCASE
 * Component: https://21st.dev/@prebuiltui/components/cards/marquee-cards-with-hover-effect
 *
 * Spec:
 * - Marquee of cards styled like the hero (brand-violet cards, white type).
 * - Mix of services offered + projects worked on: some image cards, some
 *   info-only cards.
 *
 * TODO(showcase-agent): replace this stub with the marquee. Content list comes
 * from round-2 grilling (services + projects).
 */
export function ShowcaseSection() {
  return (
    <section
      id="work"
      className="flex min-h-screen flex-col items-center justify-center gap-8 bg-background p-8 text-foreground"
    >
      <h2 className="text-3xl font-semibold tracking-tight md:text-5xl">Work</h2>
    </section>
  );
}
